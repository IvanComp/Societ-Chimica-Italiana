@model IEnumerable<sci.Models.TabSex>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescrSex)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SelSex)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescrSex)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SelSex)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodSex }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodSex }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodSex })
        </td>
    </tr>
}

</table>
