@model IEnumerable<sci.Models.TabNaz>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescrNaz)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SelNaz)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescrNaz)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SelNaz)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodNaz }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodNaz }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodNaz })
        </td>
    </tr>
}

</table>
